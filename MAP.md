> Regenerated 2026-09-19 by /init-workflow. Refreshed by /weekly-reconciliation.

# Repo map

The **tree is derived** (generated by command, overwritten wholesale — never hand-edit it). The **annotations are curated** — update a row when a path's purpose changes; /weekly-reconciliation prunes rows for dead paths and flags new unannotated ones.

## Tree (derived)

```
.
├── .claude/
│   └── skills/
│       ├── end-session/
│       │   ├── scripts/
│       │   │   └── render_transcript.py
│       │   └── SKILL.md
│       └── weekly-reconciliation/
│           └── SKILL.md
├── journal/
│   └── .gitkeep
├── public/
│   └── favicon.svg
├── src/
│   ├── sim/
│   │   ├── hex.test.ts
│   │   ├── hex.ts
│   │   ├── rng.test.ts
│   │   └── rng.ts
│   ├── ui/
│   │   ├── App.tsx
│   │   └── app.css
│   └── main.tsx
├── .gitignore
├── .oxlintrc.json
├── CLAUDE.md
├── README.md
├── STATUS.md
├── decisions.md
├── index.html
├── package-lock.json
├── package.json
├── spec.md
├── tsconfig.app.json
├── tsconfig.json
├── tsconfig.node.json
└── vite.config.ts
```

## What each path holds (curated)

| Path | What it holds |
|---|---|
| `README.md` | Orientation + resume read-order |
| `CLAUDE.md` | How we work here; Derived Facts table |
| `spec.md` | Design of record + future direction |
| `STATUS.md` | Current state (overwritten each session) |
| `decisions.md` | Append-only "chose X over Y because Z" |
| `MAP.md` | This file |
| `journal/` | One committed summary per session |
| `sessions/` | Raw transcripts (gitignored, locally greppable) |
| `references/` | Third-party source material (blog-post PDFs); gitignored, cite don't paste |
| `src/sim/` | Pure-TS deterministic simulation: RNG, hex grid, (later) worlds, ships, orders, dispatches, reports |
| `src/ui/` | React + SVG front end; reads only the PlayerView, never ground truth |
| `src/main.tsx` | Browser entry point |
| `index.html` | Vite HTML shell |
| `public/` | Static assets served as-is |
| `package.json` | Scripts and dependencies (see CLAUDE.md for the command table) |
| `vite.config.ts` | Vite + Vitest config |
| `tsconfig*.json` | TypeScript project references (app + node) |
| `.oxlintrc.json` | Lint rules |
| `.claude/skills/` | Per-repo skills: end-session, weekly-reconciliation |
