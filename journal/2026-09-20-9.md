# 2026-09-20 — Session 9: Phase 1b built

Transcript: `sessions/2026-09-20-9.transcript.md`

## What was done

Housekeeping first: merged PR #41 (Phase 1 design docs) and PR #42 (1a code) — the latter's `Closes` line had only closed `#18`, so `#19 #20 #22–#26` were closed by hand; fast-forwarded `main` from 74fdb90 to b7ad774; removed the `phase0-prototype` and `phase1-milestones` worktrees and their local and remote branches. Then a fresh worktree `phase1b` off `main`.

Two design notes from the user went into `spec.md` and `decisions.md` before code: **revolt as a garrison contest** (weekly attrition, governor flees or dies at zero, world becomes independent; the same ground model for the Warlord's troops; marine beachheads for landings) and **the Empire taxes trade routes, not planets** (client rulers, garrisons hold the port and the palace, off-route worlds are financial liabilities, extractive taxation is an emergency order; independent worlds regrow a garrison). Issue `#35` was rewritten from a per-world tax yield to a trade-route flow; `#28 #30 #31` got the notes.

Then the 1b prototype, eight slices, each committed with tests (74 → 116): data model (ground contests, marines, ship damage/troops/passengers, factions with seats and stances, save v3); world states `#28`; combat `#30` and pirates `#29 #43 #47`; troops `#31`; scouts as agents `#33` — the user's mid-session note "agents are scouts now" replaced the separate agent characters with a stay on the scout order; the Warlord `#32`; the UI `#21` (colour is allegiance, age the badge; lane packets in the dossier; watch and transport in the orders dialog; prizes on the books). A screenshot pass found the Warlord's governors' letters showing in the player's inbox — `buildPlayerView` now filters by addressee. An 8-seed 80-week soak found raiders being seized while *passing through* ports and dying at the fleet's seat; both fixed. Draft **PR #52** opened; it closes nine issues on merge.

## Decisions (rationale in decisions.md)

- Revolt is a week-by-week contest, not a switch.
- The Empire taxes trade routes, not planets; garrisons hold port and palace.
- Scouts are the agents (supersedes agents as characters).
- A dozen prototype calls recorded together as *Building 1b*, each an open question in `spec.md`.

## Issues

- Closed: `#19 #20 #22 #23 #24 #25 #26` (delivered by PR #42).
- Opened: `#53` (settle the twelve 1b questions in the playtest).
- Commented: `#11` (what remains: escort, blockade, survey, governor orders), `#28 #30 #31 #35`.
- PR #52 closes `#21 #28 #29 #30 #31 #32 #33 #43 #47` on merge. `#34` is next.
